name: Build kernels

on:
  workflow_dispatch:

jobs:
  matrix:
    name: Generate matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}
    steps:
      - uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-25.11
      - uses: actions/checkout@v6
      - name: Build matrix
        id: matrix
        env:
          RUNNER_LABEL: "ubuntu-24.04"
          SUFFIX: |
            {
                "aarch64-linux": "ubuntu-24.04-arm",
                "x86_64-linux": "ubuntu-24.04"
            }
        run: |
          matrix="$(
            nix eval --json .#packages | \
              jq -e --argjson suffix "$SUFFIX" '[. | to_entries | .[] | .key as $key | foreach (.value | keys | .[]) as $package (null; {"platform": $key, "runs-on": "\($runs-on[$key])", package: $package})]'
          )"
          matrix="$(jq -n '{"platform": "x86_64-linux", "runs-on": "ubuntu-24.04", "package": "linux_7_0"}]')"
          separator="matrix-$RANDOM"
          {
            echo "matrix<<$separator"
            echo "$matrix" | jq -e .
            echo "$separator"
          } >> "$GITHUB_OUTPUT"
          cat "$GITHUB_OUTPUT"

  build:
    name: Build kernel
    needs: matrix
    strategy:
      matrix:
        include: ${{ fromJson(needs.matrix.outputs.matrix) }}
    runs-on: "${{ matrix.runs-on }}"
    steps:
      - uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            max-jobs = 1
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/checkout@v6
      - name: Build
        env:
          PACKAGE: ${{ matrix.package }}
          PLATFORM: ${{ matrix.platform }}
        run: |
          OUTPUT_NAMES="$(nix eval --json ".#packages.${PLATFORM}.${PACKAGE}.outputs" | jq -r '.[]')"
          mapfile -t OUTPUTS <<<"$OUTPUT_NAMES"
          TARGETS=()
          for output in "${OUTPUTS[@]}"; do
            TARGETS=(
              "${TARGETS[@]}"
              ".#packages.${PLATFORM}.${PACKAGE}.${output}"
            )
          done
          echo "Building ${TARGETS[*]}"
          nix build "${TARGETS[@]}" --print-out-paths --print-build-logs
